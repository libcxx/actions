name: Build Runtimes

on: push

jobs:
  debug:
    runs-on: [libcxx-builder]
    steps:
    - name: "Show Workspace"
      run: |
        ls -lart
        pwd
        ls
    - uses: actions/checkout@v2
      with:
        path: all-actions
    - uses: ./all-actions/build


