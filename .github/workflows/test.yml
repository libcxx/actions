name: Build Runtimes

on: push

jobs:
  debug:
    runs-on: [libcxx-builder]
    steps:
    - name: "Show Workspace"
      run: |
        ls -lart
        pwd
        ls
    - uses: actions/checkout@v2
    - uses: ./all-actions/build


